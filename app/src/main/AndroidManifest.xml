<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HifzApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddStudent"
            android:label="Add Activity">
        </activity>
        <activity
            android:name=".ListOfStudentsActivity"
            android:label="List of students Activity">
        </activity>
        <activity
            android:name=".StudentDetailsActivity"
            android:label="Student Details Activity">
        </activity>
        <activity
            android:name=".UpdateStudentActivity"
            android:label="Update Activity">
        </activity>
        <activity
            android:name=".AssignTasks"
            android:label="Assign Tasks Activity">
        </activity>
        <activity
            android:name=".PreviousHistory"
            android:label="Previous History Activity">
        </activity>
    </application>

</manifest>